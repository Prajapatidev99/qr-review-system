# Agent Behavioral Rules

## Git Commit Protocol
- **ALWAYS ask for explicit user confirmation BEFORE running any `git commit` or `git push` commands.**
- Present the list of modified/added files and the proposed commit message to the user first.
- Do NOT run `git commit` or `git push` automatically without the user's explicit consent.
